<%= form_with(model: message, local: true) do |form| %>
  <% if message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% message.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="input-group">
  <%=form.text_field :content, class: "form-control form-control-lg",placeholder: "Message",style:";position: relative;width:calc(100%-15px);margin-top:10%"%>
  <%= form.submit "Add", class: "btn btn-outline-secondary",style:"position:absolute;margin-top:12%;margin-left:100%" %>
<!--  <button class="btn btn-outline-secondary" type="button" id="button-add">Button</button>-->
</div>
<% end %>